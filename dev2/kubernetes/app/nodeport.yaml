apiVersion: v1
kind: Service
metadata:
  name: app2-nodeport
spec:
  type: NodePort
  selector:
    app: app2-deployment
  ports:
  - name: web-service
    port: 8090
    protocol: TCP
    targetPort: 8090
