apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin-service
  namespace: mysql-database
spec:
  selector:
    app: phpmyadmin
  type: LoadBalancer
  ports:
    - name: phpmyadmin
      protocol: TCP
      port: 80
      targetPort: 8080
